NEXT_PUBLIC_SITE_URL=http://localhost:3000

# JWT secret used when issuing/verifying tokens
JWT_SECRET=change-me-in-production

# Database connection (overridden automatically when using Docker)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sports_store

# SMTP settings for password reset emails (Gmail example)
# Để dùng Gmail, bạn cần tạo App Password tại: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=baohq25513@gmail.com
SMTP_PASSWORD=your-16-char-app-password
MAIL_FROM="SportStore <baohq25513@gmail.com>"

